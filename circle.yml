machine:
  java:
    version: oraclejdk7
test:
  pre:
    - curl -kL -o Records.zip https://students.cs.byu.edu/~cs240ta/summer2015/download/record-indexer-data.zip 
    - unzip -u Records.zip
  override:
    - ant clean
    - ant import
    - ant server-tests
    - ant server
    - ant client-tests
